README directory layers

Put configuration file and layer specific files in this directory.

The move this directory outside the PorPOISe directory, 
so you can keep configuration and layer specific files 
separate from the PorPOISe source distribution.

Files to put here:

config.xml - PorPOISe configuration
POI filter subclasses
WebApp subclasses
WebApp templates
XML poi files
